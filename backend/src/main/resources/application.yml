spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://db.rdnlqxazzofplalgvqjz.supabase.co:5432/postgres}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:CkPKRAtgJeJBMK2d}
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    open-in-view: false

  flyway:
    enabled: false

server:
  port: ${PORT:8080}
  error:
    include-message: never

app:
  jwt:
    secret: ${JWT_SECRET:defaultSecretKeyForLocalDevMustBeAtLeast32BytesLong123456}
    access-minutes: ${JWT_EXPIRATION_MINUTES:30}
    refresh-days: 14

  cors:
    allowed-origins: ${ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173,https://*.onrender.com}